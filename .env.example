# ------------------------------
# 1. Database Configuration (Supabase)
# ------------------------------
# URL สำหรับเชื่อมต่อ Database (Transaction Pooler - Port 6543)
DATABASE_URL="postgres://[user]:[password]@[host]:6543/[db_name]?pgbouncer=true"

# URL สำหรับทำ Migration (Direct Connection - Port 5432)
DIRECT_URL="postgres://[user]:[password]@[host]:5432/[db_name]"

# ------------------------------
# 2. Server Configuration
# ------------------------------
# พอร์ตที่ Backend จะรัน
PORT=8080

# ------------------------------
# 3. Security & Session (สำคัญมาก!)
# ------------------------------
# คีย์ลับสำหรับเข้ารหัส Session Cookie (ตั้งอะไรก็ได้แต่ต้องยาวและเดายาก)
SESSION_SECRET="replace_with_a_long_random_secure_string"

# ------------------------------
# 4. CORS Configuration
# ------------------------------
# URL ของ Frontend ที่อนุญาตให้ส่ง Cookie มาหา Backend ได้
# สำคัญมากสำหรับระบบ Session เพราะเราใช้ credentials: true
CORS_ORIGIN="http://localhost:3000"