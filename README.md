# Real Estate Backend API 🏠

This is the backend service for the Real Estate application, built with **NestJS**, **Prisma ORM**, and **PostgreSQL** (Supabase).

It provides RESTful APIs for managing properties, users, and favorites. It also includes an **Auto-Seeding** feature that automatically populates the database with sample property data and agent details upon the first run.

## 🛠️ Tech Stack
- **Framework:** NestJS
- **Database:** PostgreSQL (via Supabase)
- **ORM:** Prisma
- **Language:** TypeScript

## 🚀 Getting Started

### 1. Prerequisites
- Node.js (v18 or later)
- npm (Node Package Manager)
- A PostgreSQL Database URL (Local or Cloud like Supabase/Neon)

### 2. Installation

Clone the repository and install dependencies:

```bash
git clone <your-repo-url>
cd real-estate-backend
npm install